1.При любом презапуске или падениия процесса все задачи теряются 
2.Нельзя восстановить данные
3.Упирается в лимиты памяти
4.Любой поиск или фильтрация по атрибутам задачи — O(n), и при росте числа элементов это станет узким место

сотояние гонки:
 при одновременных запросах несколько клиентов могут одновременно считать текущее состояние tasks.json, вычислить одинаковый следующий отправить свои изменения
  
Дублирование или пропуск id.

Потеря обновлений: один PATCH может перезаписать изменения другого.

Неконсистентность и повреждение списка задач.